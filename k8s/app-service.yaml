apiVersion: v1
kind: Service
metadata:
  name: orders-service
  namespace: orders-desk
  labels:
    app: orders-app
spec:
  type: NodePort
  ports:
  - port: 8080
    targetPort: 8080
    nodePort: 30080  # Accessible at http://195.206.233.23:30080
    protocol: TCP
    name: http
  selector:
    app: orders-app

